{
  "name": "@better-skill-capped/dagger",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "echo 'Dagger module ready'",
    "test": "echo 'No tests configured for Dagger module'",
    "type-check": "bunx tsc --noEmit -p tsconfig.check.json",
    "lint": "bunx eslint src",
    "format": "bunx prettier --check src",
    "format:write": "bunx prettier --write src",
    "typecheck": "bunx tsc --noEmit -p tsconfig.check.json"
  },
  "dependencies": {
    "@shepherdjerred/dagger-utils": "^0.7.0",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.0",
    "eslint": "^10.0.0",
    "prettier": "^3.7.4",
    "typescript-eslint": "^8.48.1"
  },
  "dagger": {}
}
